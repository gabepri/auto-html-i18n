# auto-dom-i18n
Zero-dependency i18n library that automatically translates DOM text via Mutation observer. Features smart variable masking, inline tag preservation, and context-aware resolution (gender/tone). Works with React, Vue, Svelte, and &amp; Vanilla JS without static keys.
